<?xml version="1.0"?>
<!-- This file details any extensions needed to the data model to store data from this source, everything else is automatically generated from the model description so this is all we need to do to add to the model. -->

<classes>
	<class name="APOTerm" extends="OntologyTerm" is-interface="true"/>
	<class name="Observable" extends="APOTerm" is-interface="true">
	</class>
	<class name="MutantType" extends="APOTerm" is-interface="true">
	</class>
	<class name="Qualifier" extends="APOTerm" is-interface="true">
	</class>
	<class name="ExperimentType" extends="APOTerm" is-interface="true">
	</class>
</classes>
